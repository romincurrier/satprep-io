import { supabase } from './supabase.js';
const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));let busy=false;
async function ctx(){const{data:{session}}=await supabase.auth.getSession();if(!session)return{};const{data:p}=await supabase.from('profiles').select('id,role,household_id,first_name').eq('id',session.user.id).maybeSingle();let students=[];if(p?.role==='student'){const{data:s}=await supabase.from('students').select('*').eq('profile_id',p.id).maybeSingle();if(s)students=[s]}else if(p?.role==='parent'&&p.household_id){const{data:s}=await supabase.from('students').select('*').eq('household_id',p.household_id).order('created_at');students=s||[]}return{session,profile:p,students}}
function form(student){return `<div class="eyebrow">ACADEMIC RECORDS</div><h2>Use previous test results</h2><p class="muted">Upload a CTP, ERB, FAST, MAP, PSAT, SAT or other standardized assessment, or enter the important scores manually. SATprep.io will preserve these results as part of the student's learning profile.</p><div id="assessmentMsg"></div><div class="field"><label>Assessment</label><select id="assessmentType"><option>CTP</option><option>ERB</option><option>FAST</option><option>MAP Growth</option><option>PSAT</option><option>PSAT/NMSQT</option><option>SAT</option><option>Other</option></select></div><div style="display:grid;grid-template-columns:1fr 1fr;gap:12px"><div class="field"><label>Test date (optional)</label><input id="assessmentDate" type="date"></div><div class="field"><label>Overall score (optional)</label><input id="overallScore" placeholder="e.g. 1280 or scale score"></div></div><div class="field"><label>Overall percentile (optional)</label><input id="overallPercentile" type="number" min="0" max="100" placeholder="e.g. 78"></div><div class="field"><label>Section/domain results</label><textarea id="sectionScores" rows="4" placeholder="Example: Quantitative Reasoning 62nd percentile; Reading Comprehension 84th percentile; Vocabulary 71st percentile"></textarea></div><div class="field"><label>Upload report (optional)</label><input id="assessmentFile" type="file" accept="application/pdf,image/png,image/jpeg,image/webp"><div class="small">PDF, PNG, JPG or WebP · maximum 10 MB. Reports are stored privately.</div></div><div class="field"><label>Notes (optional)</label><textarea id="assessmentNotes" rows="2" placeholder="Anything useful about this test or the student's performance"></textarea></div><button class="btn" id="saveAssessment">Save assessment</button>`}
async function save(student,container){if(busy)return;busy=true;const btn=container.querySelector('#saveAssessment');btn.disabled=true;btn.textContent='Saving…';try{const{data:{session}}=await supabase.auth.getSession(),file=container.querySelector('#assessmentFile').files?.[0];let file_path=null,file_name=null,source_method='manual';if(file){if(file.size>10*1024*1024)throw new Error('The report must be 10 MB or smaller.');const ext=(file.name.split('.').pop()||'file').replace(/[^a-z0-9]/gi,'').toLowerCase(),safe=`${crypto.randomUUID()}.${ext}`,path=`${session.user.id}/${student.id}/${safe}`;const{error:up}=await supabase.storage.from('assessment-reports').upload(path,file,{contentType:file.type,upsert:false});if(up)throw up;file_path=path;file_name=file.name;source_method='upload'}const percentile=container.querySelector('#overallPercentile').value,sections=container.querySelector('#sectionScores').value.trim();const{error}=await supabase.from('prior_assessments').insert({student_id:student.id,assessment_type:container.querySelector('#assessmentType').value,assessment_date:container.querySelector('#assessmentDate').value||null,grade_level:student.grade_level||null,source_method,file_name,file_path,overall_score:container.querySelector('#overallScore').value.trim()||null,overall_percentile:percentile===''?null:Number(percentile),section_scores:sections?{reported_text:sections}:{},notes:container.querySelector('#assessmentNotes').value.trim()||null,created_by:session.user.id,status:'submitted'});if(error)throw error;container.innerHTML=`<div class="success"><strong>Assessment saved.</strong><br>SATprep.io will use this academic record alongside the student's own diagnostic rather than treating either source as a replacement for the other.</div><button class="btn" id="assessmentDone" style="margin-top:14px">Continue</button>`;container.querySelector('#assessmentDone').onclick=()=>location.assign('/?app=1')}catch(e){container.querySelector('#assessmentMsg').innerHTML=`<div class="error">${esc(e.message||'Unable to save assessment.')}</div>`;btn.disabled=false;btn.textContent='Save assessment'}finally{busy=false}}
function routeToAssessment(student){location.assign(`/?app=1&priorAssessment=1&student=${encodeURIComponent(student.id)}`)}
async function renderRoute(){const p=new URLSearchParams(location.search);if(p.get('priorAssessment')!=='1')return false;const{profile,students}=await ctx();if(!profile||!students.length)return true;let student=null;const requested=p.get('student');if(profile.role==='student')student=students[0];else if(requested)student=students.find(s=>s.id===requested);if(!student&&profile.role==='parent'&&students.length===1)student=students[0];const app=document.querySelector('#app');if(!student&&profile.role==='parent'){app.innerHTML=`<div class="top"><div class="logo">SAT<span>prep.io</span></div><div class="navlinks"><button class="linkbtn" id="assessmentBack">Dashboard</button></div></div><main class="wrap"><section class="card"><div class="eyebrow">ACADEMIC RECORDS</div><h1>Choose a student</h1>${students.map(s=>`<button class="option chooseAssessmentStudent" data-id="${s.id}">${esc(s.display_name||[s.first_name,s.last_name].filter(Boolean).join(' ')||'Student')}</button>`).join('')}</section></main>`;document.querySelector('#assessmentBack').onclick=()=>location.assign('/?app=1');document.querySelectorAll('.chooseAssessmentStudent').forEach(b=>b.onclick=()=>routeToAssessment(students.find(s=>s.id===b.dataset.id)));return true}if(!student){app.innerHTML=`<main class="wrap"><section class="card"><div class="error">We could not find that student record.</div><button class="btn" id="assessmentBack">Return to dashboard</button></section></main>`;document.querySelector('#assessmentBack').onclick=()=>location.assign('/?app=1');return true}app.innerHTML=`<div class="top"><div class="logo">SAT<span>prep.io</span></div><div class="navlinks"><button class="linkbtn" id="assessmentBack">Dashboard</button></div></div><main class="wrap"><section class="card" style="max-width:760px;margin:auto" id="assessmentForm">${form(student)}</section></main>`;document.querySelector('#assessmentBack').onclick=()=>location.assign('/?app=1');const c=document.querySelector('#assessmentForm');c.querySelector('#saveAssessment').onclick=()=>save(student,c);return true}
async function inject(){const p=new URLSearchParams(location.search);if(p.get('priorAssessment')==='1')return;if(p.get('app')!=='1'||p.get('openBilling')==='1')return;const{profile,students}=await ctx();if(!profile||!students.length)return;if(profile.role==='student'){const s=students[0];if(!s.onboarding_complete||s.diagnostic_completed_at||document.querySelector('#priorAssessmentCard'))return;const main=document.querySelector('main');if(!main)return;const card=document.createElement('section');card.id='priorAssessmentCard';card.className='card';card.style.maxWidth='760px';card.style.margin='18px auto';card.innerHTML=`<div class="eyebrow">OPTIONAL — PRIOR TESTING</div><h2>Have scores from CTP, ERB, FAST, MAP, PSAT, SAT or another test?</h2><p class="muted">Put those results to work before your SATprep.io diagnostic. Upload the report or enter the scores manually. You can also skip this and continue with the diagnostic.</p><div style="display:flex;gap:10px;flex-wrap:wrap"><button class="btn" id="addPriorScores">Add previous scores</button><button class="btn secondary" id="skipPriorScores">I don't have scores</button></div>`;main.prepend(card);card.querySelector('#addPriorScores').onclick=()=>routeToAssessment(s);card.querySelector('#skipPriorScores').onclick=()=>card.remove()}else if(profile.role==='parent'&&!document.querySelector('#academicRecordsBtn')){const section=document.querySelector('#parentDashboardEnhanced .hero .row');if(!section)return;const b=document.createElement('button');b.id='academicRecordsBtn';b.className='btn secondary';b.textContent='Academic records';b.onclick=()=>students.length===1?routeToAssessment(students[0]):location.assign('/?app=1&priorAssessment=1');section.appendChild(b)}}
async function init(){if(await renderRoute())return;await inject();const o=new MutationObserver(()=>setTimeout(inject,50));o.observe(document.documentElement,{subtree:true,childList:true})}
setTimeout(init,150);
