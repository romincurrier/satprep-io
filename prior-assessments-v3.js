import { supabase } from './supabase.js';
import * as pdfjsLib from 'pdfjs-dist/legacy/build/pdf.mjs';
import pdfWorker from 'pdfjs-dist/legacy/build/pdf.worker.mjs?url';
import {extractPdfLayout,parseAssessmentReport} from './assessment-parser.js';
pdfjsLib.GlobalWorkerOptions.workerSrc=pdfWorker;
const esc=s=>String(s??'').replace(/[&<>\"]/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;'}[m]));
let busy=false;
async function ctx(){const{data:{session}}=await supabase.auth.getSession();if(!session)return{};const{data:p}=await supabase.from('profiles').select('id,role,household_id,first_name').eq('id',session.user.id).maybeSingle();let students=[];if(p?.role==='student'){const{data:s}=await supabase.from('students').select('*').eq('profile_id',p.id).maybeSingle();if(s)students=[s]}else if(p?.role==='parent'&&p.household_id){const{data:s}=await supabase.from('students').select('*').eq('household_id',p.household_id).order('created_at');students=s||[]}return{session,profile:p,students}}
function closeOverlay(){document.querySelector('#assessmentOverlay')?.remove()}
function shell(inner){closeOverlay();const o=document.createElement('div');o.id='assessmentOverlay';o.style.cssText='position:fixed;inset:0;z-index:100000;background:rgba(13,31,52,.72);overflow:auto;padding:24px;display:flex;align-items:flex-start;justify-content:center';o.innerHTML=`<div class="card" style="width:min(860px,100%);margin:20px auto;padding:28px" id="assessmentPanel">${inner}</div>`;document.body.appendChild(o);o.addEventListener('click',e=>{if(e.target===o)closeOverlay()});return o.querySelector('#assessmentPanel')}
function uploadForm(student){const name=student.display_name||[student.first_name,student.last_name].filter(Boolean).join(' ')||'Student';return `<div class="eyebrow">ACADEMIC RECORDS</div><h2>Upload a previous test report</h2><p class="muted">Upload ${esc(name)}'s CTP, ERB, FAST, MAP, PSAT, SAT or other standardized report. SATprep.io will read the document automatically.</p><div id="assessmentMsg"></div><div class="field"><label>Report file</label><input id="assessmentFile" type="file" accept="application/pdf"><div class="small">PDF · maximum 10 MB · stored privately</div></div><div class="notice"><strong>No score entry required.</strong> The report itself is the source of truth.</div><div style="display:flex;gap:10px;flex-wrap:wrap;margin-top:16px"><button class="btn" id="processAssessment">Upload & analyze report</button><button class="btn secondary" id="closeAssessment">Cancel</button></div>`}
function results(data,fileName){const mapped=(data.sections||[]).filter(s=>s.scale_score!=null||s.percentile!=null||s.stanine!=null);return `<div class="eyebrow">REPORT PROCESSED</div><h2>✓ ${esc(fileName)} was analyzed</h2><div class="success"><strong>${esc(data.assessment_type)} recognized.</strong> ${mapped.length} score rows mapped.</div>${mapped.length?`<div class="card" style="margin-top:14px"><h3>Extracted results</h3>${mapped.map(s=>`<div class="lesson"><div><strong>${esc(s.name)}</strong>${s.percentile_basis?`<div class="small">Percentile basis: ${esc(s.percentile_basis)}</div>`:''}</div><div class="right">${s.scale_score!=null?`<span class="badge">Scale ${esc(s.scale_score)}</span>`:''}${s.percentile!=null?`<span class="badge">${esc(s.percentile)}th percentile</span>`:''}${s.stanine!=null?`<span class="badge">Stanine ${esc(s.stanine)}</span>`:''}</div></div>`).join('')}</div>`:`<div class="notice" style="margin-top:14px"><strong>The report text was extracted, but SATprep.io still could not confidently map score rows.</strong> The extracted table has been saved for parser review; no scores were guessed.</div>`}${data.strengths?.length?`<div class="success" style="margin-top:14px"><strong>Potential strengths:</strong> ${data.strengths.map(esc).join(', ')}</div>`:''}${data.needs_attention?.length?`<div class="notice" style="margin-top:14px"><strong>Areas to confirm:</strong> ${data.needs_attention.map(esc).join(', ')}</div>`:''}<div style="display:flex;gap:10px;margin-top:18px"><button class="btn" id="assessmentDone">Continue</button><button class="btn secondary" id="addAnotherAssessment">Add another report</button></div>`}
async function updateSignals(student,data){if(data.confidence==='low')return;const current=student.recommended_path&&typeof student.recommended_path==='object'?student.recommended_path:{};const prior=(Array.isArray(current.prior_assessments)?current.prior_assessments:[]).filter(x=>x.source_id!==data.source_id);prior.push({source_id:data.source_id,assessment_type:data.assessment_type,assessment_date:data.assessment_date||null,strengths:data.strengths||[],needs_attention:data.needs_attention||[],sections:(data.sections||[]).map(s=>({name:s.name,percentile:s.percentile,percentile_basis:s.percentile_basis,scale_score:s.scale_score,stanine:s.stanine}))});await supabase.from('students').update({recommended_path:{...current,prior_assessments:prior}}).eq('id',student.id)}
async function processBlob(student,blob,fileName,existingId=null,filePath=null){const panel=document.querySelector('#assessmentPanel');panel.innerHTML=`<div class="eyebrow">PROCESSING REPORT</div><h2>Reading ${esc(fileName)}…</h2><p class="muted">Reconstructing the report table and matching score rows to norm groups.</p><div class="progress"><div style="width:75%"></div></div>`;try{const layout=await extractPdfLayout(pdfjsLib,blob),data=parseAssessmentReport(layout,fileName);data.source_id=existingId||crypto.randomUUID();const payload={assessment_type:data.assessment_type,assessment_date:data.assessment_date||null,grade_level:student.grade_level||null,source_method:'upload',file_name:fileName,file_path:filePath,section_scores:{sections:data.sections,strengths:data.strengths,needs_attention:data.needs_attention},extracted_text:layout.text.slice(0,50000),extracted_data:data,status:data.confidence==='low'?'needs_review':'processed',processed_at:new Date().toISOString(),processing_error:null};let id=existingId;if(existingId){const{error}=await supabase.from('prior_assessments').update(payload).eq('id',existingId);if(error)throw error}else{const{data:row,error}=await supabase.from('prior_assessments').insert({...payload,student_id:student.id,created_by:(await supabase.auth.getSession()).data.session.user.id}).select('id').single();if(error)throw error;id=row.id;data.source_id=id}await updateSignals(student,data);panel.innerHTML=results(data,fileName);panel.querySelector('#assessmentDone').onclick=closeOverlay;panel.querySelector('#addAnotherAssessment').onclick=()=>openOverlay(student)}catch(e){if(existingId)await supabase.from('prior_assessments').update({status:'needs_review',processing_error:String(e.message||e),processed_at:new Date().toISOString()}).eq('id',existingId);panel.innerHTML=`<div class="error"><strong>We couldn't finish reading this report.</strong><br>${esc(e.message||'Processing failed.')}</div><button class="btn" id="assessmentDone">Close</button>`;panel.querySelector('#assessmentDone').onclick=closeOverlay}}
async function uploadAndProcess(student,panel){if(busy)return;const file=panel.querySelector('#assessmentFile')?.files?.[0];if(!file)return panel.querySelector('#assessmentMsg').innerHTML='<div class="error">Choose a PDF report first.</div>';if(file.size>10*1024*1024)return panel.querySelector('#assessmentMsg').innerHTML='<div class="error">The report must be 10 MB or smaller.</div>';busy=true;try{const{data:{session}}=await supabase.auth.getSession(),path=`${session.user.id}/${student.id}/${crypto.randomUUID()}.pdf`;const{error}=await supabase.storage.from('assessment-reports').upload(path,file,{contentType:'application/pdf'});if(error)throw error;await processBlob(student,file,file.name,null,path)}finally{busy=false}}
async function findPending(student){const{data}=await supabase.from('prior_assessments').select('*').eq('student_id',student.id).eq('source_method','upload').is('processed_at',null).not('file_path','is',null).order('created_at',{ascending:false}).limit(1);return data?.[0]||null}
async function processPending(student,row){const panel=document.querySelector('#assessmentPanel');panel.innerHTML=`<div class="eyebrow">PROCESSING EXISTING REPORT</div><h2>Analyzing ${esc(row.file_name||'uploaded report')}…</h2><p class="muted">You do not need to upload it again.</p><div class="progress"><div style="width:65%"></div></div>`;const{data:blob,error}=await supabase.storage.from('assessment-reports').download(row.file_path);if(error)throw error;await processBlob(student,blob,row.file_name||'Assessment.pdf',row.id,row.file_path)}
async function openOverlay(student){const panel=shell(uploadForm(student));panel.querySelector('#closeAssessment').onclick=closeOverlay;panel.querySelector('#processAssessment').onclick=()=>uploadAndProcess(student,panel);const pending=await findPending(student);if(pending)await processPending(student,pending)}
function chooseStudent(students){const panel=shell(`<div class="eyebrow">ACADEMIC RECORDS</div><h2>Choose a student</h2>${students.map(s=>`<button class="option chooseAssessmentStudent" data-id="${s.id}" style="width:100%;margin:8px 0">${esc(s.display_name||'Student')}</button>`).join('')}<button class="btn secondary" id="closeAssessment">Cancel</button>`);panel.querySelector('#closeAssessment').onclick=closeOverlay;panel.querySelectorAll('.chooseAssessmentStudent').forEach(b=>b.onclick=()=>openOverlay(students.find(s=>s.id===b.dataset.id)))}
async function inject(){const p=new URLSearchParams(location.search);if(p.get('app')!=='1'||p.get('openBilling')==='1')return;const{profile,students}=await ctx();if(!profile||!students.length)return;if(profile.role==='student'){const s=students[0];if(!s.onboarding_complete||s.diagnostic_completed_at||document.querySelector('#priorAssessmentCard'))return;const main=document.querySelector('main');if(!main)return;const card=document.createElement('section');card.id='priorAssessmentCard';card.className='card';card.style.maxWidth='760px';card.style.margin='18px auto';card.innerHTML=`<div class="eyebrow">OPTIONAL — PRIOR TESTING</div><h2>Have a previous standardized test report?</h2><p class="muted">Upload it and SATprep.io will read the scores automatically. No retyping required.</p><div style="display:flex;gap:10px"><button class="btn" id="addPriorScores">Upload previous report</button><button class="btn secondary" id="skipPriorScores">I don't have scores</button></div>`;main.prepend(card);card.querySelector('#addPriorScores').onclick=()=>openOverlay(s);card.querySelector('#skipPriorScores').onclick=()=>card.remove()}else if(profile.role==='parent'&&!document.querySelector('#academicRecordsBtn')){const section=document.querySelector('#parentDashboardEnhanced .hero .row');if(!section)return;const b=document.createElement('button');b.id='academicRecordsBtn';b.className='btn secondary';b.textContent='Academic records';b.onclick=()=>students.length===1?openOverlay(students[0]):chooseStudent(students);section.appendChild(b)}}
let scheduled=false;function schedule(){if(scheduled)return;scheduled=true;setTimeout(async()=>{scheduled=false;await inject()},80)}const o=new MutationObserver(schedule);o.observe(document.documentElement,{subtree:true,childList:true});setTimeout(inject,250);supabase.auth.onAuthStateChange(schedule);
