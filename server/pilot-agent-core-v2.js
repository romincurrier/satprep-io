import {createHash,randomBytes} from 'node:crypto';
import {eligibleQuestions} from '../question-bank-production.js';
import {STAGED_PRACTICE_BANK} from '../practice-bank-v2.js';
import {skillEligibleForExam} from '../sat-spec.js';
import {service} from './supabase-server.js';

export const PILOT_AGENT_VERSION='pilot-agent-v1';
export const PILOT_PERSONAS=Object.freeze({
 balanced_middle:{label:'Balanced middle',grade:10,targetExam:'SAT',targetScore:1250,rw:.64,math:.64},
 math_strong_rw_weak:{label:'Math strong / R&W developing',grade:11,targetExam:'SAT',targetScore:1350,rw:.46,math:.88},
 rw_strong_math_weak:{label:'R&W strong / Math developing',grade:10,targetExam:'PSAT/NMSQT',targetScore:1250,rw:.88,math:.46},
 foundation:{label:'Foundation learner',grade:9,targetExam:'PSAT/NMSQT',targetScore:1100,rw:.40,math:.38},
 advanced:{label:'Advanced learner',grade:11,targetExam:'SAT',targetScore:1500,rw:.91,math:.91}
});
const clamp=x=>Math.max(.08,Math.min(.97,x));
const digest=value=>parseInt(createHash('sha256').update(String(value)).digest('hex').slice(0,8),16)/0xffffffff;
const now=()=>new Date().toISOString();
const makePassword=()=>`P!lot-${randomBytes(18).toString('base64url')}9a`;
const syntheticEmail=(role,token)=>`satprep-pilot-${role}-${token}@example.com`;
const probability=(p,q,phase,index)=>clamp((q.section==='MATH'?p.math:p.rw)-((Number(q.difficulty||2)-2)*.10)+(phase==='practice'?.05:0)+(index>=14?-.04:0));
const isCorrect=(p,q,phase,index,seed)=>digest(`${seed}|${phase}|${q.id}|${index}`)<probability(p,q,phase,index);
const chosen=(q,correct,seed,index)=>correct?q.answerIndex:(q.answerIndex+1+(Math.floor(digest(`${seed}|wrong|${q.id}|${index}`)*3)%3))%4;
const eligible=(q,exam)=>(!q.exams||q.exams.includes(exam))&&skillEligibleForExam(q.skill,exam);

function diagnosticPlan(exam){const bank=eligibleQuestions(exam).filter(q=>q.format==='mcq'&&Array.isArray(q.choices)&&Number.isInteger(q.answerIndex));const take=section=>{const rows=bank.filter(q=>q.section===section),out=[],seen=new Set();for(const q of rows){if(!seen.has(q.skill)){out.push(q);seen.add(q.skill)}if(out.length===10)break}for(const q of rows){if(out.length>=10)break;if(!out.includes(q))out.push(q)}return out};return[...take('RW'),...take('MATH')]}
function practicePlan(skill,exam){return STAGED_PRACTICE_BANK.filter(q=>q.skill===skill&&eligible(q,exam)&&Array.isArray(q.choices)&&Number.isInteger(q.answerIndex)).slice(0,5)}
function weakest(rows,exam){return[...rows].filter(x=>skillEligibleForExam(x.skill_key,exam)).sort((a,b)=>Number(a.mastery)-Number(b.mastery)).slice(0,2).map(x=>x.skill_key)}
async function write(path,body,{returnRows=false,upsert=false}={}){const prefer=[upsert?'resolution=merge-duplicates':null,returnRows?'return=representation':'return=minimal'].filter(Boolean).join(',');return service(path,{method:'POST',headers:{Prefer:prefer},body:JSON.stringify(body)})}
async function patch(path,body){return service(path,{method:'PATCH',headers:{Prefer:'return=minimal'},body:JSON.stringify(body)})}
async function logEvent(runId,type,key,payload={}){await write('/rest/v1/test_events',{test_run_id:runId,event_type:type,event_key:key,payload:{pilot_agent_version:PILOT_AGENT_VERSION,...payload}})}
async function createAuthUser({email,password,role,firstName,lastName,parentAuthorized=false}){return service('/auth/v1/admin/users',{method:'POST',body:JSON.stringify({email,password,email_confirm:true,user_metadata:{role,first_name:firstName,last_name:lastName,...(role==='student'?{date_of_birth:'2010-01-15'}:{})},app_metadata:{satprep_test_account:true,account_origin:'pilot_agent',...(parentAuthorized?{satprep_parent_authorized:true}:{})}})})}
async function verifySignIn(email,password){const result=await service('/auth/v1/token?grant_type=password',{method:'POST',body:JSON.stringify({email,password})});return !!result?.access_token&&!!result?.user?.id}

export async function createPilotFamily(adminProfileId,personaKey){const persona=PILOT_PERSONAS[personaKey];if(!persona)throw Object.assign(new Error('Unknown pilot persona.'),{status:400});const seed=randomBytes(6).toString('hex'),parentEmail=syntheticEmail('parent',seed),studentEmail=syntheticEmail('student',seed),parentPassword=makePassword(),studentPassword=makePassword();let parentUser,studentUser,householdId;try{parentUser=await createAuthUser({email:parentEmail,password:parentPassword,role:'parent',firstName:'Pilot',lastName:`Parent ${seed.slice(0,4)}`});const parents=await service(`/rest/v1/profiles?id=eq.${encodeURIComponent(parentUser.id)}&select=id,household_id&limit=1`),parent=parents?.[0];if(!parent?.household_id)throw new Error('Pilot parent household was not created.');householdId=parent.household_id;await patch(`/rest/v1/profiles?id=eq.${encodeURIComponent(parent.id)}`,{is_test_account:true});await patch(`/rest/v1/households?id=eq.${encodeURIComponent(householdId)}`,{name:`Pilot ${persona.label} ${seed.slice(0,4)}`,is_test_household:true,student_limit:3});studentUser=await createAuthUser({email:studentEmail,password:studentPassword,role:'student',firstName:'Pilot',lastName:`Student ${seed.slice(0,4)}`,parentAuthorized:true});await patch(`/rest/v1/profiles?id=eq.${encodeURIComponent(studentUser.id)}`,{is_test_account:true,household_id:householdId,date_of_birth:'2010-01-15'});const students=await service(`/rest/v1/students?profile_id=eq.${encodeURIComponent(studentUser.id)}&select=id&limit=1`),student=students?.[0];if(!student?.id)throw new Error('Pilot student record was not created.');await patch(`/rest/v1/students?id=eq.${encodeURIComponent(student.id)}`,{household_id:householdId,is_test_student:true,test_label:`${PILOT_AGENT_VERSION}:${personaKey}`,first_name:'Pilot',last_name:`Student ${seed.slice(0,4)}`,display_name:`Pilot Student ${seed.slice(0,4)}`,date_of_birth:'2010-01-15',grade_level:persona.grade,math_course:persona.math>.75?'Algebra II':'Algebra I',target_exam:persona.targetExam,target_score:persona.targetScore,onboarding_complete:true});await write('/rest/v1/parent_students?on_conflict=parent_profile_id,student_id',{parent_profile_id:parent.id,student_id:student.id},{upsert:true});const runs=await write('/rest/v1/test_runs',{tester_profile_id:adminProfileId,test_student_id:student.id,run_type:'full_journey',label:`${PILOT_AGENT_VERSION} · ${persona.label}`},{returnRows:true}),run=runs?.[0];if(!run?.id)throw new Error('Pilot test run could not be created.');const parentAuth=await verifySignIn(parentEmail,parentPassword),studentAuth=await verifySignIn(studentEmail,studentPassword);await logEvent(run.id,'pilot_family_created',personaKey,{household_id:householdId,parent_profile_id:parent.id,student_id:student.id,parent_auth_verified:parentAuth,student_auth_verified:studentAuth,target_exam:persona.targetExam,target_score:persona.targetScore});return{run,persona,seed,household_id:householdId,parent:{profile_id:parent.id,email:parentEmail,auth_verified:parentAuth},student:{id:student.id,profile_id:studentUser.id,email:studentEmail,auth_verified:studentAuth}}}catch(error){if(studentUser?.id)await service(`/auth/v1/admin/users/${encodeURIComponent(studentUser.id)}`,{method:'DELETE'}).catch(()=>{});if(parentUser?.id)await service(`/auth/v1/admin/users/${encodeURIComponent(parentUser.id)}`,{method:'DELETE'}).catch(()=>{});if(householdId)await service(`/rest/v1/households?id=eq.${encodeURIComponent(householdId)}`,{method:'DELETE'}).catch(()=>{});throw error}}

export async function runPilotDiagnostic(family){const {run,student,persona,seed}=family,plan=diagnosticPlan(persona.targetExam);if(plan.length<20)throw new Error('Pilot diagnostic fixture bank is not deep enough.');const attempts=await write('/rest/v1/diagnostic_attempts',{student_id:student.id,status:'in_progress',summary:{engine:PILOT_AGENT_VERSION,test_only:true,persona:persona.label,question_plan:plan.map(q=>q.id)}},{returnRows:true}),attempt=attempts?.[0];if(!attempt?.id)throw new Error('Pilot diagnostic attempt could not be created.');const bySkill=new Map(),sections={RW:{right:0,total:0},MATH:{right:0,total:0}};for(let i=0;i<plan.length;i++){const q=plan[i],correct=isCorrect(persona,q,'diagnostic',i,seed),selected=chosen(q,correct,seed,i),responseMs=35000+Math.floor(digest(`${seed}|time|${q.id}`)*65000);await write('/rest/v1/diagnostic_responses',{attempt_id:attempt.id,student_id:student.id,question_key:q.id,domain:q.domain,skill_key:q.skill,difficulty:q.difficulty,selected_answer:selected,correct_answer:q.answerIndex,is_correct:correct,response_ms:responseMs,scored_by_server:true});sections[q.section].total++;if(correct)sections[q.section].right++;const row=bySkill.get(q.skill)||{right:0,total:0,domain:q.domain,section:q.section};row.total++;if(correct)row.right++;bySkill.set(q.skill,row)}const skillRows=[...bySkill].map(([skill,v])=>({skill_key:skill,mastery:v.total?v.right/v.total:0,items_attempted:v.total,domain:v.domain,section:v.section}));for(const row of skillRows)await write('/rest/v1/skill_mastery?on_conflict=student_id,skill_key',{student_id:student.id,skill_key:row.skill_key,mastery:row.mastery,items_attempted:row.items_attempted,updated_at:now()},{upsert:true});const rw=sections.RW.right/sections.RW.total,math=sections.MATH.right/sections.MATH.total,overall=(rw+math)/2,priority=[...skillRows].sort((a,b)=>a.mastery-b.mastery).slice(0,6).map(x=>({skill:x.skill_key,mastery:x.mastery,source:'pilot diagnostic'})),strengths=[...skillRows].sort((a,b)=>b.mastery-a.mastery).slice(0,4).map(x=>({skill:x.skill_key,mastery:x.mastery,source:'pilot diagnostic'})),completed=now(),recommended=overall>=.8?'Accelerated SAT/PSAT Path':overall>=.55?'Core SAT/PSAT Path':'Foundation-Building Path';await patch(`/rest/v1/diagnostic_attempts?id=eq.${encodeURIComponent(attempt.id)}`,{status:'completed',completed_at:completed,math_score:math,rw_score:rw,overall_score:overall,recommended_start:recommended,summary:{engine:PILOT_AGENT_VERSION,test_only:true,persona:persona.label,question_plan:plan.map(q=>q.id),priority_skills:priority,strengths}});await patch(`/rest/v1/students?id=eq.${encodeURIComponent(student.id)}`,{diagnostic_completed_at:completed,diagnostic_math_mastery:math,diagnostic_rw_mastery:rw,recommended_path:{priority_skills:priority,strengths,diagnostic_version:PILOT_AGENT_VERSION,test_only:true,recommended_start:recommended}});await logEvent(run.id,'pilot_diagnostic_completed','diagnostic',{attempt_id:attempt.id,total:plan.length,rw_mastery:rw,math_mastery:math,overall,priority_skills:priority.map(x=>x.skill)});return{attempt_id:attempt.id,total:plan.length,rw,math,overall,skillRows,priority}}

export async function runPilotPractice(family,diagnostic){const {run,student,persona,seed}=family,sessions=[];for(const skill of weakest(diagnostic.skillRows,persona.targetExam)){const plan=practicePlan(skill,persona.targetExam);if(!plan.length)continue;const existing=diagnostic.skillRows.find(x=>x.skill_key===skill)||{mastery:0,items_attempted:0};let right=0;for(let i=0;i<plan.length;i++){const q=plan[i],correct=isCorrect(persona,q,'practice',i,seed),selected=chosen(q,correct,seed,i);if(correct)right++;await write('/rest/v1/question_attempts',{student_id:student.id,lesson_key:`pilot-${skill}`,question_index:i,skill_key:skill,selected_answer:selected,correct_answer:q.answerIndex,is_correct:correct,response_ms:25000+Math.floor(digest(`${seed}|practice-time|${q.id}`)*55000)})}const oldN=Number(existing.items_attempted||0),newN=oldN+plan.length,newMastery=((Number(existing.mastery||0)*oldN)+right)/Math.max(1,newN),completed=now();await write('/rest/v1/skill_mastery?on_conflict=student_id,skill_key',{student_id:student.id,skill_key:skill,mastery:newMastery,items_attempted:newN,updated_at:completed},{upsert:true});await write('/rest/v1/lesson_progress?on_conflict=student_id,lesson_key',{student_id:student.id,lesson_key:`pilot-${skill}`,current_question:0,best_score:right/plan.length,last_score:right/plan.length,completed_at:completed,updated_at:completed},{upsert:true});const result={skill,questions:plan.length,correct:right,score:right/plan.length,mastery_before:Number(existing.mastery||0),mastery_after:newMastery};sessions.push(result);await logEvent(run.id,'pilot_practice_completed',skill,result)}await patch(`/rest/v1/test_runs?id=eq.${encodeURIComponent(run.id)}`,{status:'completed',completed_at:now(),notes:`${PILOT_AGENT_VERSION} completed diagnostic plus ${sessions.length} targeted practice session(s).`});return sessions}
export async function runPilotAgent(adminProfileId,personaKey){const family=await createPilotFamily(adminProfileId,personaKey),diagnostic=await runPilotDiagnostic(family),practice=await runPilotPractice(family,diagnostic);return{version:PILOT_AGENT_VERSION,run_id:family.run.id,persona:personaKey,persona_label:family.persona.label,household_id:family.household_id,parent:family.parent,student:family.student,diagnostic:{total:diagnostic.total,rw_mastery:diagnostic.rw,math_mastery:diagnostic.math,overall:diagnostic.overall,priority_skills:diagnostic.priority.map(x=>x.skill)},practice}}
