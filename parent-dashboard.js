import { supabase } from "./supabase.js";
const esc=s=>String(s??"").replace(/[&<>\"]/g,m=>({"&":"&amp;","<":"&lt;",">":"&gt;",'\"':"&quot;"}[m]));let rendering=false;
async function getContext(){const{data:{session}}=await supabase.auth.getSession();if(!session)return null;const{data:profile}=await supabase.from("profiles").select("id,email,first_name,last_name,role,household_id").eq("id",session.user.id).maybeSingle();if(!profile||profile.role!=="parent"||!profile.household_id)return null;const{data:students}=await supabase.from("students").select("id,profile_id,first_name,last_name,display_name,grade_level,target_exam,target_score,onboarding_complete,diagnostic_completed_at,created_at").eq("household_id",profile.household_id).order("created_at",{ascending:true});return{session,profile,students:students||[]}}
async function studentSummary(student){const[{data:progress},{data:skills},{data:attempts}]=await Promise.all([supabase.from("lesson_progress").select("lesson_key,best_score,completed_at").eq("student_id",student.id),supabase.from("skill_mastery").select("skill_key,mastery,items_attempted").eq("student_id",student.id),supabase.from("question_attempts").select("id,is_correct,created_at").eq("student_id",student.id).order("created_at",{ascending:false}).limit(50)]);const completed=(progress||[]).filter(x=>x.completed_at).length,masteryValues=(skills||[]).map(x=>Number(x.mastery||0)),avgMastery=masteryValues.length?Math.round(masteryValues.reduce((a,b)=>a+b,0)/masteryValues.length*100):0,weakest=(skills||[]).slice().sort((a,b)=>Number(a.mastery)-Number(b.mastery))[0],strongest=(skills||[]).slice().sort((a,b)=>Number(b.mastery)-Number(a.mastery))[0],recent=attempts||[],accuracy=recent.length?Math.round(recent.filter(x=>x.is_correct).length/recent.length*100):0;return{completed,avgMastery,weakest,strongest,accuracy,attemptCount:recent.length}}
function studentCard(student,summary){const name=student.display_name||[student.first_name,student.last_name].filter(Boolean).join(" ")||"Student";const accountReady=!!student.profile_id,status=!accountReady?"Login not activated":student.diagnostic_completed_at?"Learning path active":student.onboarding_complete?"Ready for diagnostic":"Student setup needed";return `<article class="card" style="padding:22px"><div class="row"><div><div class="eyebrow" style="margin-bottom:8px">STUDENT</div><h2 style="margin:0 0 5px">${esc(name)}</h2><div class="small">Grade ${student.grade_level||"—"} · ${esc(student.target_exam||"PSAT")} · Target ${student.target_score||"—"}</div></div><span class="badge ${student.diagnostic_completed_at?"good":"warn"}">${esc(status)}</span></div><div class="grid" style="margin-top:18px"><div class="c3"><div class="label">Lessons</div><div class="metric" style="font-size:24px">${summary.completed}</div></div><div class="c3"><div class="label">Mastery</div><div class="metric" style="font-size:24px">${summary.avgMastery}%</div></div><div class="c3"><div class="label">Recent accuracy</div><div class="metric" style="font-size:24px">${summary.attemptCount?summary.accuracy+"%":"—"}</div></div><div class="c3"><div class="label">Questions</div><div class="metric" style="font-size:24px">${summary.attemptCount}</div></div></div><hr><div class="row"><div class="small">${!accountReady?"Activate the student's login so they can begin their diagnostic and learning journey.":summary.strongest?`Strongest: <strong>${esc(summary.strongest.skill_key)}</strong>`:"Performance insights will appear after the student begins working."}${summary.weakest?`<br>Needs attention: <strong>${esc(summary.weakest.skill_key)}</strong>`:""}</div><div style="display:flex;gap:8px;flex-wrap:wrap">${!accountReady?`<button class="btn activate-student" data-student="${student.id}">Activate student login</button>`:""}<button class="btn secondary student-detail" data-student="${student.id}">View student</button></div></div></article>`}
async function authedPost(url,body){const{data:{session}}=await supabase.auth.getSession();if(!session)throw new Error('Please sign in again.');const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json',Authorization:`Bearer ${session.access_token}`},body:JSON.stringify(body)}),data=await r.json().catch(()=>({}));if(!r.ok)throw new Error(data.error||'Request failed.');return data}
function activationModal(student){const name=student.display_name||[student.first_name,student.last_name].filter(Boolean).join(' ')||'Student';const overlay=document.createElement('div');overlay.style.cssText='position:fixed;inset:0;background:rgba(13,31,52,.58);z-index:9999;display:flex;align-items:center;justify-content:center;padding:20px';overlay.innerHTML=`<div class="card" style="width:min(560px,100%);padding:26px"><div class="eyebrow">ACTIVATE STUDENT LOGIN</div><h2>${esc(name)}</h2><p class="muted">Create the login this student will use for SATprep.io. This connects to the student record you already created; it will not create a second child.</p><div id="activateError"></div><div class="field"><label>Student email</label><input id="studentLoginEmail" type="email" placeholder="student@example.com" autocomplete="email"></div><div class="field"><label>Temporary password</label><input id="studentLoginPassword" type="password" minlength="8" autocomplete="new-password"></div><p class="small">Use an email the student can access, or a parent-managed address. The password must be at least 8 characters.</p><div class="notice"><strong>Parent authorization:</strong> By activating this login, you authorize SATprep.io to create and maintain this student's account and learning data as part of your household subscription.</div><div style="display:flex;gap:10px;margin-top:16px"><button class="btn" id="activateStudentConfirm">Activate login</button><button class="btn secondary" id="activateStudentCancel">Cancel</button></div></div>`;document.body.appendChild(overlay);overlay.querySelector('#activateStudentCancel').onclick=()=>overlay.remove();overlay.querySelector('#activateStudentConfirm').onclick=async()=>{const btn=overlay.querySelector('#activateStudentConfirm'),email=overlay.querySelector('#studentLoginEmail').value.trim(),password=overlay.querySelector('#studentLoginPassword').value;if(!email||password.length<8){overlay.querySelector('#activateError').innerHTML='<div class="error">Enter a valid email and a password of at least 8 characters.</div>';return}btn.disabled=true;btn.textContent='Activating…';try{await authedPost('/api/activate-student-login',{student_id:student.id,email,password});overlay.innerHTML=`<div class="card" style="width:min(560px,100%);padding:26px"><div class="success"><strong>Student login activated.</strong><br>${esc(name)} can now sign in with <strong>${esc(email)}</strong>.</div><p>The student's first steps will be to complete their learner profile and take the short diagnostic that builds their personalized learning path.</p><button class="btn" id="activationDone">Done</button></div>`;overlay.querySelector('#activationDone').onclick=()=>location.reload()}catch(e){btn.disabled=false;btn.textContent='Activate login';overlay.querySelector('#activateError').innerHTML=`<div class="error">${esc(e.message)}</div>`}}}
async function render(){if(rendering)return;const params=new URLSearchParams(location.search);if(params.get("app")!=="1"||params.get("openBilling")==="1"||params.get("onboarding")==="child")return;const ctx=await getContext();if(!ctx)return;const main=document.querySelector("main");if(!main||document.querySelector("#parentDashboardEnhanced"))return;rendering=true;try{const summaries=await Promise.all(ctx.students.map(studentSummary));main.id="parentDashboardEnhanced";main.className="wrap";main.innerHTML=`<section class="hero"><div class="row" style="align-items:flex-start"><div><div class="eyebrow">PARENT DASHBOARD</div><h1>Welcome, ${esc(ctx.profile.first_name||"Parent")}.</h1><p>See each student's preparation, progress and next steps in one place.</p></div><button class="btn" id="manageBilling">Plans & Billing</button></div></section><section class="grid"><div class="card c3"><div class="label">Students</div><div class="metric">${ctx.students.length}</div></div><div class="card c3"><div class="label">Household plan</div><div class="metric" style="font-size:21px">${ctx.students.length>1?"Family":"Individual"}</div></div><div class="card c3"><div class="label">Active learners</div><div class="metric">${ctx.students.filter(s=>s.profile_id).length}</div></div><div class="card c3"><div class="label">Account</div><div class="metric" style="font-size:17px">Parent</div></div></section><section style="display:grid;gap:16px;margin-top:18px">${ctx.students.length?ctx.students.map((s,i)=>studentCard(s,summaries[i])).join(""):`<div class="card"><h2>Add your first student</h2><p class="muted">Your family account is ready. Add a student to begin building a personalized SAT or PSAT learning path.</p><button class="btn" id="addFirstStudent">Add student</button></div>`}</section><section class="card" style="margin-top:18px"><div class="row"><div><h2 style="margin-bottom:5px">Family & account</h2><div class="small">Manage students, billing, privacy and account access.</div></div><button class="btn secondary" id="addStudentFromDashboard">Add another student</button></div></section>`;document.querySelector("#manageBilling")?.addEventListener("click",()=>location.assign("/?app=1&openBilling=1"));document.querySelector("#addFirstStudent")?.addEventListener("click",()=>location.assign("/?app=1&onboarding=child"));document.querySelector("#addStudentFromDashboard")?.addEventListener("click",()=>location.assign("/?app=1&onboarding=child"));document.querySelectorAll('.activate-student').forEach(btn=>btn.addEventListener('click',()=>activationModal(ctx.students.find(s=>s.id===btn.dataset.student))));document.querySelectorAll(".student-detail").forEach(btn=>btn.addEventListener("click",()=>showStudent(ctx.students.find(s=>s.id===btn.dataset.student),summaries[ctx.students.findIndex(s=>s.id===btn.dataset.student)])))}finally{rendering=false}}
function showStudent(student,summary){const main=document.querySelector("main");if(!main||!student)return;const name=student.display_name||[student.first_name,student.last_name].filter(Boolean).join(" ")||"Student";main.innerHTML=`<section class="hero"><div class="row"><div><div class="eyebrow">STUDENT OVERVIEW</div><h1>${esc(name)}</h1><p>Grade ${student.grade_level||"—"} · ${esc(student.target_exam||"PSAT")} · Target ${student.target_score||"—"}</p></div><button class="btn secondary" id="backParent">Back to parent dashboard</button></div></section><section class="grid"><div class="card c3"><div class="label">Login</div><div class="metric" style="font-size:20px">${student.profile_id?'Active':'Not activated'}</div></div><div class="card c3"><div class="label">Diagnostic</div><div class="metric" style="font-size:20px">${student.diagnostic_completed_at?'Complete':'Not started'}</div></div><div class="card c3"><div class="label">Average mastery</div><div class="metric">${summary.avgMastery}%</div></div><div class="card c3"><div class="label">Recent accuracy</div><div class="metric">${summary.attemptCount?summary.accuracy+"%":"—"}</div></div><div class="card c12"><h2>Next step</h2><p class="muted">${!student.profile_id?'Activate the student login so the student can sign in and begin the diagnostic.':!student.diagnostic_completed_at?'The student is ready to sign in, finish their learner profile and take the initial diagnostic.':'The diagnostic is complete and the personalized learning journey is active.'}</p>${!student.profile_id?`<button class="btn" id="activateFromDetail">Activate student login</button>`:''}</div></section>`;document.querySelector("#backParent")?.addEventListener("click",()=>{main.removeAttribute("id");render()});document.querySelector('#activateFromDetail')?.addEventListener('click',()=>activationModal(student))}
const observer=new MutationObserver(()=>setTimeout(render,0));observer.observe(document.documentElement,{childList:true,subtree:true});setTimeout(render,150);supabase.auth.onAuthStateChange(()=>setTimeout(render,150));
